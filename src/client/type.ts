interface RootState {
    auth: {
        token: string
    }
}
interface GameIdProps {
    gameId: number;
 }
 
interface NavButtonProps {
    text: string;
 }
 

type SwitchForm = {
    switchForm: any
}